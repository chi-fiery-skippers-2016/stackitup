<br>
<br>
<div class="container">
  <br>
  <br>
  <h1>Tags</h1>
  <div class="tag_meta">
    <% @tags.each do |tag| %>
      <span class="tag_box">
        <a href = "/tags/<%= tag.id %>"><%= tag.name %></a>
      </span>
    <% end %>
  </div>
</div>
